# Copy to .env (gitignored) or set as Aspire parameters.
# Never put a real token in this file or anywhere else in the repo.

# Railway account or workspace token for aspire deploy
RAILWAY_TOKEN=

# Optional: adopt an existing project/environment instead of creating new ones
RAILWAY_PROJECT_ID=
RAILWAY_ENVIRONMENT_ID=
